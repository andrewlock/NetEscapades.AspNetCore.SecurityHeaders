<Project>
  <PropertyGroup>
    <PackageReleaseNotes Condition="'$(VersionPrefix)' == '0.24.0'">
      <![CDATA[
Features:

* Allow adding multiple uris to CSP builder `AddFrameAncestors()` #179
* Add support for additional directives on `Permissions-Policy` header #177 (Thanks [@Registeel](https://github.com/Registeel)!)
]]>
    </PackageReleaseNotes>
     <PackageReleaseNotes>$(PackageReleaseNotes)
See $(PackageProjectUrl)/blob/master/CHANGELOG.md#v$(VersionPrefix.Replace('.','')) for more details.</PackageReleaseNotes>
  </PropertyGroup>
</Project>